# URL base de la API del backend (incluye el prefijo /api).
# Desarrollo local:
# VITE_API_URL=http://localhost:3000/api
# Producción: reemplazar por la URL pública del backend desplegado.
VITE_API_URL=https://tu-backend-en-produccion.com/api
